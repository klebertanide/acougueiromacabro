services:
  - type: web
    name: acougueiromacabro
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --workers 1 --bind 0.0.0.0:$PORT --timeout 180
    envVars:
      - key: PORT
        value: 10000
